{
  "type": "module",
  "private": true,
  "scripts": {
    "build": "vite build",
    "build:staging": "vite build --mode staging",
    "build:preview": "vite build --mode preview",
    "build:canary": "vite build --mode production-canary",
    "build:staging-east": "vite build --mode staging.east",
    "build:staging_east": "vite build --mode staging_east",
    "dev": "vite"
  },
  "devDependencies": {
    "vite-plugin-extra-app-config": "workspace:^",
    "vite-plugin-inspect": "^11.1.0"
  }
}
